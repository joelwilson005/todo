package com.joel.todo.model;

// RoleType is an enumeration that represents the different types of roles that a user can have in the system
public enum RoleType {
    // USER represents a standard user role
    USER
}